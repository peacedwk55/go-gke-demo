---
galaxy_info:
  author: santiphap
  role_name: nginx_container
  description: NGINX in Docker, serving a Jinja2-templated page from a read-only bind mount
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
dependencies:
  - role: docker
